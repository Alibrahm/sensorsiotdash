import React from "react";
import Lottie from "lottie-react";
import noData from "../../public/nodata.json";

const Nodata = () => <Lottie animationData={noData} loop={true} />;

export default Nodata;